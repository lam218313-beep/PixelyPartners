version: '3.9'

services:
  # ============================================
  # ORCHESTRATOR: Analysis Engine
  # ============================================
  orchestrator:
    build:
      context: .
      dockerfile: Dockerfile.orchestrator
    container_name: pixely_orchestrator
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_MODEL=${OPENAI_MODEL}
      - ORCHESTRATOR_USER=${ORCHESTRATOR_USER}
      - ORCHESTRATOR_PASSWORD=${ORCHESTRATOR_PASSWORD}
      - PIXELY_OUTPUTS_DIR=/app/orchestrator/outputs
    volumes:
      - ./orchestrator/outputs:/app/orchestrator/outputs
      - ./orchestrator:/app/orchestrator
    networks:
      - pixely_network
    command: python -m orchestrator all
    restart: unless-stopped

  # ============================================
  # FRONTEND: Streamlit Dashboard
  # ============================================
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    container_name: pixely_frontend
    ports:
      - "8501:8501"
    environment:
      - PIXELY_OUTPUTS_DIR=/app/orchestrator/outputs
    volumes:
      - ./orchestrator/outputs:/app/orchestrator/outputs
      - ./frontend:/app/frontend
    networks:
      - pixely_network
    command: streamlit run frontend/app.py --server.port=8501 --server.address=0.0.0.0
    depends_on:
      - orchestrator

  # ============================================
  # API: FastAPI Analytics Server
  # ============================================
  api:
    build:
      context: .
      dockerfile: Dockerfile.orchestrator  # Usamos el mismo dockerfile del orquestador por ahora, tiene python+librerias
    container_name: pixely_api
    ports:
      - "8000:8000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_MODEL=${OPENAI_MODEL}
      - PIXELY_OUTPUTS_DIR=/app/orchestrator/outputs
    volumes:
      - ./orchestrator:/app/orchestrator
      - ./api:/app/api
      - ./orchestrator/outputs:/app/orchestrator/outputs
    command: uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload
    networks:
      - pixely_network
    depends_on:
      - orchestrator

  # ============================================
  # COMMENTED OUT: Database, Nginx, Certbot
  # Activate these when implementing persistence and security layers
  # ============================================

  # db:
  #   image: postgres:15-alpine
  #   container_name: pixely_db
  #   environment:
  #     - POSTGRES_USER=${DB_USER}
  #     - POSTGRES_PASSWORD=${DB_PASSWORD}
  #     - POSTGRES_DB=${DB_NAME}
  #   volumes:
  #     - db_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U ${DB_USER}"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5
  #   networks:
  #     - pixely_network

  # nginx:
  #   image: nginx:latest
  #   container_name: pixely_nginx
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #   depends_on:
  #     - frontend
  #     - api
  #   networks:
  #     - pixely_network

  # certbot:
  #   image: certbot/certbot
  #   container_name: pixely_certbot
  #   volumes:
  #     - ./letsencrypt:/etc/letsencrypt
  #   networks:
  #     - pixely_network

networks:
  pixely_network:
    driver: bridge

# volumes:
#   db_data:
